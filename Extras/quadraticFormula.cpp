#include <stdio.h>
using namespace std;
int square(int x) {
    return x * x;
}

int main(int argc, char const *argv[]) {
    // // Create a function to find only the root value of x in any quadratic equation ax ^ 2 + bx + c.
    // // The function will take three arguments : a,b,c
    // // Find the discriminant (b ^ 2 - 4ac)
    // int a, b, c;
    // //multiply 2 numbers and add them
    // int discriminant = square(b) - 4 * a * c;
    // //if discriminant is less than 0, return 0
    // if (discriminant < 0) {
    //     return 0;
    // }
    // //return the value of x
    // return (-b + discriminant) / (2 * a);

    return 0;
}